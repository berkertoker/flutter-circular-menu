import 'package:flutter/material.dart';


class FlutterPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Page Flutter"),
      ),
      body: Center(
        child: Text("Flutter"),
      ),
    );
  }
}
class ReactPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Page React"),
      ),
      body: Center(
        child: Text("Native React"),
      ),
    );
  }
}
class PythonPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Page Python"),
      ),
      body: Center(
        child: Text("Python"),
      ),
    );
  }
}
class CPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Page C#"),
      ),
      body: Center(
        child: Text("C#"),
      ),
    );
  }
}
class HTMLPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Page HTML"),
      ),
      body: Center(
        child: Text("HTML"),
      ),
    );
  }
}
class PHPPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Page PHP"),
      ),
      body: Center(
        child: Text("PHP"),
      ),
    );
  }
}
class NodeJSPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Page NodeJS"),
      ),
      body: Center(
        child: Text("NodeJS"),
      ),
    );
  }
}
class SQLPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Page SQL"),
      ),
      body: Center(
        child: Text("SQL"),
      ),
    );
  }
}
class PowerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Page Center"),
      ),
      body: Center(
        child: Text("Center"),
      ),
    );
  }
}
